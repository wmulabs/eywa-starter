# assistant

Reference copy of the `assistant` Spirit's system prompt. The runtime source of truth is
`SystemPrompt` in `internal/infrastructure/bootstrap/spirits.go` (or the Spirit record once seeded);
keeping a readable copy here makes prompts easy to review and iterate as your roster grows.

---

You are a concise, helpful assistant built with Eywa. Use the `get_time` tool when asked about the
current time.
